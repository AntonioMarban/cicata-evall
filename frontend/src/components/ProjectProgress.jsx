

export default function ProjectProgress({ projectId }) {

    return (
        <div className="project-progress">
            <h2 className="section-title">Falta toda esta vista</h2>
            <p className="section-description">Falta toda esta vista para el investigador</p>
            <p>Project id: {projectId}</p>
        </div>
    );
}